# Placeholder de test end-to-end

**Objetivo**: verificar que al cambiar de skin en el dashboard
(`packages/dashboard/`), los organismos referenciados en cada
`skins/*.json` efectivamente se renderizan sin error, incluyendo el nuevo
`PermissionsCenterPanel` agregado en v0.0.5.

**TODO**: implementar con Playwright u otra herramienta de e2e una vez
que exista un entorno de staging real del dashboard.
